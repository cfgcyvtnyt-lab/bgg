# 이 파일을 .env로 복사하고 키를 채운다. .env는 커밋되지 않는다.
# npm start가 알아서 읽는다 (node --env-file-if-exists, Node 20.6+ 내장 기능).

# 게임을 새로 추가할 때 긱에서 정보를 받아오는 데 쓴다. 없으면 그 기능만 안 된다.
BGG_API_KEY=

# 개발 중엔 비워둔다(Vite 개발서버가 앱을 띄운다).
# 도커에서는 Dockerfile이 /app/web-dist로 지정한다.
WEB_DIST=
